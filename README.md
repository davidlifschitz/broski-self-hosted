# broski-self-hosted
Self-hosted Broski replacement — Mac WebSocket bridge + native iOS SwiftUI client. Multi-backend (Claude Code, OpenCode, custom), RTT-adaptive batching, mDNS discovery, zero cloud.
